language: go

install:
  - grep 'import' -rl ./| grep ".go"| xargs sed -i 's#"github.com/dad-go/#"github.com/dad-go/dad-go/#g'
  - go get github.com/Masterminds/glide
  - glide install

go:
  - 1.8.x
